# This is a comment.
# colon + identifier( ':' IDENTIFIER ) are ignored just like python.

clase Persona haz
  func constructor(name): Persona haz
    _name = name
    ret self
  fin

  func devuelve_nombre(): Caracteres haz
    ret _name
  fin

  func cambia_nombre(name): Vacio haz
    _name = name
  fin

  _name : Caracteres
fin

